import 'package:flutter/material.dart';

class RecievedPackagesScreen extends StatelessWidget {
  const RecievedPackagesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('this is recieved packages screen'),
    );
  }
}
